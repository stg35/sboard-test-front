import { styled } from 'styled-components';

export const Input = styled.input`
	padding: 7px 15px;

	border: none;
	margin-bottom: 20px;
	outline-color: #7653fc;
	background-color: #f5f6f8;
	box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.05);
	border-radius: 5px;

	font-size: 16px;
	line-height: 22px;
`;
